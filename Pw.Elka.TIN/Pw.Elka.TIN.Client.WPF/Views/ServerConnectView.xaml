<UserControl x:Class="Pw.Elka.TIN.Client.WPF.Views.ServerConnectView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Pw.Elka.TIN.Client.WPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel>
        <StackPanel Margin="100,50,100,0">
            <Label Content="Połącz się z serwerem" FontSize="21.333"></Label>
            <Label Content="Adres serwera" />
            <TextBox Name="txtAddress" Text="localhost"/>
            <Label Content="Port serwera" />
            <TextBox Name="txtPort" Text="7777" />
            <Label Content="Nazwa użytkownika"/>
            <TextBox Name="txtUsername" Text="user"/>
            <Label Content="Hasło"/>
            <PasswordBox Name="txtPassword" Password="test"/>
            <Button Name="btnConnect" Click="btnConnect_Click" Content="Połącz" Margin="0,10,0,0" />
        </StackPanel>
    </StackPanel>
</UserControl>
